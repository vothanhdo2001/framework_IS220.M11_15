@model IEnumerable<WebMusic_Auth.Models.CategoryModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CaName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CaName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
