@model IEnumerable<WebMusic_Auth.Models.CommentsModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CoStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Song)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Content)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CoStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Song.MId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
